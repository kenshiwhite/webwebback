<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="./styles/admin.css">
</head>
<body>
    <header>
        <h1>Admin Panel</h1>
        <nav>
            <ul>
                <li><a href="<%= lang === 'en' ? '/login' : '/login?lang=ru' %>" class="a"><%= lang === 'ru' ? 'Выйти' : 'Logout' %></a></li>
                <form action="<%= lang === 'en' ? '/adminCarousel?lang=en' : '/adminCarousel?lang=ru' %>" class="form-inline ml-auto">
                    <div class="form-group">
                        <label for="langSelect" class="mr-2"style="color: white;"><%= lang === 'ru' ? 'Язык:' : 'Language:' %></label>
                        <select class="form-control" id="langSelect" name="lang" onchange="this.form.submit()">
                            <option value="en" <%= lang === 'en' ? 'selected' : '' %>>EN</option>
                            <option value="ru" <%= lang === 'ru' ? 'selected' : '' %>>RU</option>
                        </select>
                    </div>
                </form>
            </ul>
        </nav>
    </header>
    <main>
        <div id="crud">
            <ul>
                <li><button><a href="/add?lang=<%= lang %>" class="cud"><%= lang === 'ru' ? 'Добавить пользователя' : 'Add user' %></a></button></li>
                <li><button><a href="/update?lang=<%= lang %>" class="cud"><%= lang === 'ru' ? 'Обновить пользователя' : 'Update user' %></a></button></li>
                <li><button><a href="/delete?lang=<%= lang %>" class="cud"><%= lang === 'ru' ? 'Удалить пользователя' : 'Delete user' %></a></button></li>
                <li><button><a href="/adminCarousel?lang=<%= lang %>" class="a"><%= lang === 'ru' ? 'Посмотреть элементы' : 'See elements' %></a></button></li>
                <li><button><a href="/adminEdit?lang=<%= lang %>" class="a"><%= lang === 'ru' ? 'Изменить элементы' : 'Change elements' %></a></button></li>
            </ul>
        </div>
        <div id="side">
            <% if (products && products.length > 0) { %>
                <% products.forEach((product, index) => { %>
                    <% if (product.deletionDate === null){ %>
                        <h3><%= product.name %></h3>
                        <div id="concrete<%= index %>" class="carousel slide" data-bs-ride="carousel" data-bs-interval="5000">
                            <div class="carousel-inner">
                                <div class="carousel-item active">
                                    <img src="<%= product.url1 %>" class="d-block w-100" alt="">
                                    <div class="carousel-caption d-none d-md-block">
                                    </div>
                                </div>
                        
                                <div class="carousel-item">
                                    <img src="<%= product.url2 %>" class="d-block w-100" alt="">
                                    <div class="carousel-caption d-none d-md-block">
                                    </div>
                                </div>
                        
                                <div class="carousel-item">
                                    <img src="<%= product.url3 %>" class="d-block w-100" alt="">
                                    <div class="carousel-caption d-none d-md-block">
                                    </div>
                                </div>
                            </div>
                        
                            <button class="carousel-control-prev" type="button" data-bs-target="#concrete<%= index %>" data-bs-slide="prev">
                                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                <span class="visually-hidden"><%= lang === 'ru' ? 'Назад' : 'Previous' %></span>
                            </button>
                            <button class="carousel-control-next" type="button" data-bs-target="#concrete<%= index %>" data-bs-slide="next">
                                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                <span class="visually-hidden"><%= lang === 'ru' ? 'Вперед' : 'Next' %></span>
                            </button>
                        </div>
                        <p><%= product.description %></p>
                    <% } %>
                <% }) %>
            <% } %>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 Yernar's Company</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>